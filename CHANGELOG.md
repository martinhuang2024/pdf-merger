# Changelog

## v1.12.0 - 2026-08-06

- 每張紙選擇 2 頁或 4 頁時，每個已上傳檔案都有獨立的「同頁重複填滿」開關。
- 開啟的檔案會讓每個原始頁面各自建立一張 A4，並重複填滿全部格位。
- 未開啟的檔案仍依原本順序將不同頁面排入同一張紙。

## v1.11.0 - 2026-08-06

- 新增每張紙 1、2、4 頁排版選項，合併預覽與下載共用設定。
- 2 頁輸出橫向 A4 並左右排列；4 頁輸出直向 A4 並採 2×2 排列。
- 每頁保留原始比例及旋轉方向，放大到格位可容納的最大尺寸且不裁切。

## v1.10.0 - 2026-08-05

- 單獨預覽 PDF 時新增逐頁刪除操作，合併預覽不顯示刪除按鈕。
- 刪除後重新產生該檔案並立即更新清單頁數、預覽及後續合併內容。
- 加入刪除確認並限制 PDF 至少保留一頁。

## v1.9.2 - 2026-08-05

- 修正 QPDF 回傳代碼 3（完成但有警告）時被誤判為密碼或加密格式錯誤。
- 真正的密碼錯誤仍會被拒絕。

## v1.9.1 - 2026-07-30

- 批次解密每份檔案開始與完成時會讓瀏覽器先重繪，避免進度停住。
- 解密視窗新增完成份數與百分比進度條。

## v1.9.0 - 2026-07-30

- 新增 JPG、PNG、WebP、HEIC／HEIF 圖片選取與整頁拖放。
- 圖片在瀏覽器本機轉換為單頁 PDF，能與 PDF 混合排序、預覽、旋轉及合併。
- 圖片最長邊限制為 3000 像素，依原始直橫方向置中於 A4 頁面。
- 多張圖片採逐張轉換，原有 PDF 仍沿用 Safari 已驗證的直接並行讀取流程。
- 檔案清單以 `IMG` 標示圖片來源，進度條同時涵蓋 PDF 與圖片。

## v1.8.2 - 2026-07-30

- 恢復最初版本已驗證可在 Safari 使用的直接 `file.arrayBuffer()` 讀取。
- 恢復以 `Promise.allSettled()` 並行讀取多份 PDF，不再逐份等待。
- 移除檔案讀取逾時包裝、`FileReader` 備援與額外畫面等待。
- 進度改為每完成一份 PDF 更新，保留份數、百分比與檔名顯示。

## v1.8.1 - 2026-07-30

- Safari 與 iOS 優先使用 `Blob.arrayBuffer()` 讀取從「檔案」或 iCloud 選取的 PDF。
- 在 PDF 分析前主動讓出畫面更新時機，使目前檔名與「分析中」狀態能正常顯示。
- 新增檔案讀取逾時與全流程 `finally` 收尾，錯誤時會恢復選檔操作並顯示提示。

## v1.8.0 - 2026-07-30

- 移除首次開啟時不必要的 PDF.js、PDF Worker 與 QPDF WebAssembly 載入。
- 預覽引擎與解密引擎改為第一次使用對應功能時才按需載入。
- 新增 PDF 本機讀取進度，顯示目前檔名、處理份數及整體百分比。
- 已有檔案時追加 PDF，也會在清單上方顯示進度。

## v1.7.0 - 2026-07-29

- 新增加密 PDF 多選與「批次去除密碼」操作
- 同一組密碼會依序套用至所有勾選檔案，避免解密引擎同時執行造成衝突
- 批次中成功的檔案會立即解鎖，失敗的檔案會保留勾選以便改用其他密碼重試
- 保留既有單檔解密與單獨下載流程，並優化 5.4 吋手機上的批次解密視窗

## v1.6.2 - 2026-07-29

- 整個頁面都能接收拖入的 PDF。
- 已顯示檔案清單時，仍可拖放追加更多 PDF。
- 區分外部檔案拖入與清單內排序拖曳，避免兩種操作互相干擾。

## v1.6.1 - 2026-07-29

- 優化 iPhone 12 mini 等 5.4 吋裝置的解密視窗排版。
- 解密視窗支援動態視窗高度與捲動，避免螢幕鍵盤遮住密碼欄或操作按鈕。
- 手機版保留「去除密碼」文字，提升狹窄畫面上的操作辨識度。
- 長檔名改為最多兩行顯示，並加入瀏海與 Home Indicator 安全區。

## v1.6.0 - 2026-07-28

- 新增每份 PDF 的單檔預覽按鈕。
- 新增合併結果預覽，會套用目前排序與統一頁面方向設定。
- 預覽視窗顯示檔名、頁數並提供另開視窗。
- 加密 PDF 必須先去除密碼後才能預覽。

## v1.5.1 - 2026-07-28

- 方向不符的頁面改為直接旋轉 90 度。
- 不再將頁面等比例縮放置中。
- 空白頁與已有旋轉角度的頁面同樣支援方向統一。

## v1.5.0 - 2026-07-28

- 新增「統一頁面方向」選項。
- 勾選後可選擇全部直向或全部橫向。
- 未勾選時預設保留來源 PDF 的直橫混合方向。
- 方向不符的頁面會等比例縮放置中，不旋轉文字也不裁切內容。

## v1.4.1 - 2026-07-28

- 修正加入第一批 PDF 後，「加入 PDF」按鈕沒有反應。
- 將檔案選擇元件改為常駐，初次加入與後續追加共用相同入口。

## v1.4.0 - 2026-07-28

- 解鎖成功後新增清楚可見的「單獨下載」按鈕。
- 單獨下載的預設檔名改為 `原檔名_已解密.pdf`。
- 調整手機版檔案操作列，保留完整的下載按鈕文字。

## v1.3.0 - 2026-07-28

- 加密 PDF 加入後會先顯示檔案名稱與「已加密」狀態。
- 將頁面頂部的密碼欄位改為每份加密檔案的「去除密碼」按鈕。
- 密碼視窗會先顯示目前要處理的檔案名稱。
- 未解鎖的加密 PDF 會保留在清單，完成去除密碼後才可合併。

## v1.2.0 - 2026-07-28

- 新增深色模式切換按鈕。
- 首次使用會依照裝置的深色或淺色偏好自動套用。
- 使用者手動選擇後會記住顯示模式。
- 調整深色模式下的紙張、表單、檔案列、彈窗與提示訊息對比。

## v1.1.0 - 2026-07-28

- 新增已知 PDF 密碼欄位。
- 新增 QPDF WebAssembly 本機解鎖與去除密碼。
- 新增密碼必填、密碼錯誤及不支援加密格式提示。
- 新增解鎖狀態與單檔無密碼版本下載。
- 合併輸出使用解鎖後內容，不再需要來源密碼。

## v1.0.0 - 2026-07-27

- 新增多 PDF 選擇、拖放及檔案檢查。
- 新增頁數、檔案大小與合併摘要。
- 新增拖曳及按鈕排序。
- 新增自訂檔名、本機合併與下載。
- 使用本地 Vue 3 與 pdf-lib，可直接開啟 `index.html`。
